language: cpp

compiler:
  - gcc
#  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libglibmm-2.4-dev libboost-all-dev glade-gtk2 libopenjpeg-dev

before_script:
  - mkdir build
  - cd build

script:
  - cmake .. -DBUILD_BOOST:BOOST=ON
  - make

os: linux

